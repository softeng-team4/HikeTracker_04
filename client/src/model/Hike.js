export default function Hike(ascent, city, country, description, difficulty, endPoint, expectedTime, length, referencePoint, region, title, startPoint) {
    this.ascent = ascent;
    this.city = city;
    this.country = country;
    this.difficulty = difficulty;
    this.description = description;
    this.endPoint = endPoint;
    this.expectedTime = expectedTime;
    this.length = length;
    this.referencePoint = referencePoint;
    this.region = region;
    this.title = title;
    this.startPoint = startPoint;

}