export default function User(email, firstName, lastName, role, reqRole, reqStatus, respDate, hut) {
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.role = role;
    this.reqRole = reqRole;
    this.reqStatus = reqStatus;
    this.respDate = respDate;
    this.hut = hut;
}